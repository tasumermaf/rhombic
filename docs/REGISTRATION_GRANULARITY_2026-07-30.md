# REGISTERED: Granularity Bridge Ladder — Registration Cover Sheet

**Registered 2026-07-30.** The registered card is the pair of immutable documents:

1. `docs/DESIGN_GRANULARITY_BRIDGE_2026-07-21.md` (the design, as filed)
2. `docs/DIRECTOR_RULINGS_LADDER_ERRATA_REGISTRATIONS_2026-07-30.md` Part 5 (the rulings)

wherever they conflict, **the rulings govern**. Deviations after this date are dated
amendments (L-006), never silent revisions.

## Operative rulings (deltas from the design)

- **D1**: llama3.2-1b only; the scope limit goes in every claim. APPROVED.
- **D2**: ladder 12/24/~48 at 20/10/5 seeds (~240 adapters/level). Fallback 16/8/4 only if
  D9 is funded and the budget binds — D9 is deferred, so the fallback is OFF.
- **D3 RESTRICTED**: model-annotated (T3) cells admissible only with control 6.1 bounding
  their label noise AND the clean-core (T1+T2) curve reported alongside the all-classes
  curve at every level. On material divergence the clean-core curve is the claim; xsum
  topics may not carry a departure conclusion alone.
- **D4**: pool floor 1,000/class. APPROVED.
- **D5 PINNED**: ceiling := LOO acc ≥ 0.99 (≤2 errors at N=240); confirmatory endpoints =
  first level where canonical drops below 0.99 + the Δ(K) trend; **Cohen's κ reported at
  every level** (the 1.5×-chance bar degenerates to ~0.031 at K=48).
- **D6 PINNED**: TF-IDF + linear SVM data-space reference, frozen config, run per level;
  subsample size pinned at lock and reported per level.
- **D7 MANDATORY**: the split-pool control is included — "the most important ten runs in
  the design." Not optional.
- **D8 PROMOTED**: Arm B (squad-only deep ladder, 2→16 native title groups, ~144 runs) is
  funded; if budget forces a choice against D9, Arm B wins.
- **D9 DEFERRED**: no qwen L2 spot check now; revisit only if the llama curve shows a
  departure.
- **D10 LOCK FORM (Director-set)**: at every level, PASS requires
  `acc > 1.5×chance(K)` AND `perm p < 0.01` AND **κ ≥ 0.40**, with κ reported at all
  levels including L0. The 0.40 threshold is set by the Director at registration;
  objections must be argued before the card locks, not after L1 returns.

## Status

- REGISTERED. Lock requires: the D6 subsample size pinned + the cost table restated
  against measured rates (same S2-class timing-pilot discipline as the H2 registration —
  the design's "~30 min/run llama" figure is an ESTIMATE, per the Part 3 ruling that no
  measured per-run rate exists in the record).
